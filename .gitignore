# Python
__pycache__/
*.pyc
*.pyo

# Uploaded files and local database (runtime artifacts — never commit these)
backend/uploads/
backend/facts.db
backend/facts.db-shm
backend/facts.db-wal
uploads/
facts.db
facts.db-shm
facts.db-wal

# Secrets
.env
*.env

# Node (if frontend ever uses npm)
node_modules/
